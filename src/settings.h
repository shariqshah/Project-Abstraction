#ifndef settings_H_
#define settings_H_

namespace Settings
{
	bool initSettings();
	int  getWindowWidth();
	int  getWindowHeight();
	void setWindowWidth(int width);
	void setWindowHeight(int height);
}

#endif
